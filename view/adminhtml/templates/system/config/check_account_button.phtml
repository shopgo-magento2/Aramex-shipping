
<script>
    require([
        'jquery',
        'Magento_Ui/js/modal/modal'
    ], function ($,modal) {
            var options = {
                type: 'popup',
                responsive: true,
                innerScroll: true,
                title: 'Check Account',
                buttons: [{
                    text: $.mage.__('Continue'),
                    class: '',
                    click: function () {
                        this.closeModal();
                    }
                }]
            };
        window.checkAramexAccount = function(){
            $('body').append('<div class="aramex-popup"><?php echo $this->checkAccount() ?></div>');
            var popup = modal(options, $('.aramex-popup'));
            $('.aramex-popup').modal('openModal');

            $(".modal-inner-wrap").css("max-width",'35%');
        }
    });
</script>

<?php echo $block->getButtonHtml() ?>
